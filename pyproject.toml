[project]
name = "techstore-support"
version = "0.1.0"
description = "TechStore Customer Support Chatbot"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "openai-agents>=0.6.2",
]

[project.optional-dependencies]
gradio = [
    "gradio>=6.1.0",
]
